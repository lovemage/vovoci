name: Windows Release

on:
  workflow_dispatch:

jobs:
  smoke:
    runs-on: windows-latest
    steps:
      - name: Smoke test
        shell: pwsh
        run: Write-Host "windows release workflow dispatch works"
